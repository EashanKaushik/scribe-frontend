version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: amplify/code/build
    files:
      - '**/*'
  cache:
    paths:
      - amplify/code/node_modules/**/*

backend:
  phases:
    preBuild:
      commands:
        - cd amplify
        - cd code
    build:
      commands:
        - amplifyPush --simple
